#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../audio-metadata/bin/audio-metadata.js" "$@"
  ret=$?
else 
  node  "$basedir/../audio-metadata/bin/audio-metadata.js" "$@"
  ret=$?
fi
exit $ret
